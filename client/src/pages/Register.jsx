// React componeent responsible for user registration.
function Register() {
  // The return statement tells React what to display on the screen.
  return (
    // Main container for the Register page.
    <div>
      {/* Main heading of the page */}
      <h1>Create Account</h1>

      {/* Small description for the user */}
      <p>Create a new account to start ordering delicious pizza.</p>
    </div>
  );
}

// Makes this component available to other files.
export default Register;
